<!DOCTYPE html>
<html>
<head>
<title>FURNILAC PRIMAGUNA, PT.</title>
<link href="stylesheets/jquery-ui.css.erb" rel="stylesheet" type="text/css">
</head>
<body>
<h3><center><b>LAPORAN PRODUKTIVITAS PRODUKSI PERSON </b></center></h3>
<center>
  <table width="600px">
    <tr>
      <td><b>Periode : <%= params[:start_date] %> s/d <%= params[:end_date] %></b></td>
      <td><b>Karu : <%= @area.employee.name %></b></td>
      <td><b>Area : <%= @area.name %> </b> </td>
    </tr>
    <tr>
      <td><b>EH :
        <% total_eh = 0 %>
        <% @data_report_person.each do |pic| %>
          <% total_eh += (pic.total_qty  * pic.part.norms) %>
        <% end %>
        <%=  total_eh.round(2) %></b>
      </td>
      <td><b>WH :
        <% total_wh = 0 %>
        <% @data_report_person_wh.each do |pic| %>
          <% total_wh += pic.wh %>
          <% end %>
        <%=  total_wh %></b>
      </td>

    </tr>
</table>
<br>
<!-- <p>Show With Range Date -->
<center><table border="1"  width="600px">
    <tr><b>
        <td  width="80px"><b><center>Part No.</center></td>
        <td width="200px"><center><b>Part Name</td>
        <td><b><center>Qty</td>
        <td><b><center>Norms (HPU)</td>
        <td><b><center>Earned Hour</td>
    </b></tr>
    <tr>
      <tbody>
      <% @data_report_person.each do |pic| %>
        <tr>
          <td><%= pic.part.number %></td>
          <td><%= pic.part.name %></td>
          <td><%= pic.total_qty.to_i %></td>
          <td><%= pic.part.norms.round(2) %></td>
          <td><%= (pic.total_qty  * pic.part.norms).round(2) %></td>
        </tr>
      <% end %>
      </tbody>
    </tr>

</table>
<br><br><br>
<script type="text/javascript">
  $( "#start" ).datepicker({
    dateFormat: 'dd-mm-yy',
    inline: true
  });
</script>

<script type="text/javascript">
  $( "#end" ).datepicker({
    dateFormat: 'dd-mm-yy',
    inline: true
  });
</script>
</body>
</html>
