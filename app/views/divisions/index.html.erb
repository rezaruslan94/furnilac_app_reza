<p id="notice"><%= notice %></p>

<h1>Divisions</h1>

<table id="DivisionDataTables" data-source="<%= divisions_path(format: :json) %>" class="table table-bordered">
  <thead>
    <tr>
      <th>Department</th>
      <th>Division Name</th>
      <th>Employee</th>
      <th>Edit</th>
    </tr>
  </thead>

  <tbody>
  </tbody>
</table>

<br>

<%= link_to 'NEW', new_division_path, :class => "btn btn-primary btn-lg" %>
